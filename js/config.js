// js/config.js
// Supabase Project Credentials Configuration
//
// Credentials are injected automatically during build (e.g. node build.js on Cloudflare)
// or can be entered via the browser setup dialog on first launch.

window.SUPABASE_URL = "";
window.SUPABASE_PUBLISHABLE_KEY = "";
